# ClaudeBar — AI instructions

## After any code change

Always rebuild and restart the app immediately:

```bash
./run.sh
```

This kills the running instance, recompiles, and relaunches in the background. Do not skip this step — the menubar app must be running to verify that a change works correctly.
